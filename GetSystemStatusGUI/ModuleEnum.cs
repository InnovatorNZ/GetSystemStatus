namespace GetSystemStatusGUI {
    public static class ModuleEnum {
        public enum FormType {
            CPU = 0,
            RAM = 1,
            Disk = 2,
            Network = 3,
            GPU = 4
        }
    }
}